spring:
  application:
    name: GatewayService
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        fail-fast: false
    # gateway:
    #   routes:
    #     - id: user-route
    #       uri: lb://UserService
    #       predicates:
    #         - Path=/api/user/**

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
